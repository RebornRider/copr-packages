version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Pin to commit SHAs in workflow files; Dependabot opens PRs that bump
    # the SHA and update the trailing version comment.
